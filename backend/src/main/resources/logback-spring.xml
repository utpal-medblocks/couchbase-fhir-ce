<configuration>
    <!-- Remove the status listener now that we've diagnosed -->
    <!-- <statusListener class="ch.qos.logback.core.status.OnConsoleStatusListener" /> -->
    
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>

    <!-- Your Couchbase loggers -->
    <logger name="com.couchbase" level="INFO"/>
    <logger name="com.couchbase.client" level="INFO"/>
    <logger name="com.couchbase.core" level="INFO"/>
    <logger name="com.couchbase.io" level="INFO"/>
    <logger name="com.couchbase.endpoint" level="INFO"/>
    <logger name="com.couchbase.node" level="INFO"/>
    <logger name="com.couchbase.transactions" level="INFO"/>
    
    <logger name="com.couchbase.admin" level="INFO"/>
    <logger name="com.couchbase.fhir" level="INFO"/>
    <logger name="com.couchbase.common" level="INFO"/>
    
    <!-- Suppress noisy HAPI FHIR parser warnings -->
    <logger name="ca.uhn.fhir.parser.LenientErrorHandler" level="INFO"/>
    
    <!-- Suppress verbose HAPI FHIR profile loading messages -->
    <logger name="ca.uhn.fhir.context.support.DefaultProfileValidationSupport" level="INFO"/>
    
    <!-- Suppress HAPI FHIR's exception interceptor stack traces - we handle our own clean error logging -->
    <logger name="ca.uhn.fhir.rest.server.interceptor.ExceptionHandlingInterceptor" level="WARN"/>
</configuration>